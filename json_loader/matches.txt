matches obj {

    match_id, int
    match_date, date
    kickoff, time

    competition object {
        competition_id, int
        country_name, varchar
        competition_name, varchar
    }

    season obj {
        season_id, int
        season_name, varchar
    }

    home_team obj {
        team_id, int
        team_name, varchar
        team_gender, varchar
        team_group, char? varchar, null

        country object {
            id, int
            name, varchar
        }

        managers obj {
            id, int
            name, varchar
            nickname, varchar
            dob, date
            country obj {
                id, int
                name, varchar
            }
        }
    }

    away_team obj {

        team_id, int
        team_name, varchar
        team_gender, varchar
        team_group, char? varchar, null

        country object {
            id, int
            name, varchar
        }

        managers obj {
            id, int
            name, varchar
            nickname, varchar
            dob, date
            country obj {
                id, int
                name, varchar
            }
        }
    }

    home_score, int
    away_score, int
    match_week, int
    competition_stage obj {
        id, int
        name, varchar
    }

    stadium obj {
        id, int
        name, varchar
        country obj {
            id, int
            name, varchar
        }
    }

    referee obj {
        id, int
        name, varchar
        country obj{
            id, int
            name, varchar
        }
    }
}